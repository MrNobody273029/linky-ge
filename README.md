# Linky.ge (MVP starter)

Run locally:

```bash
npm install
cp .env.example .env
npm run prisma:generate
npm run prisma:migrate
npm run seed
npm run dev
```

Admin demo account (seed):
- Email: admin@linky.ge
- Password: LinkyAdmin123!
